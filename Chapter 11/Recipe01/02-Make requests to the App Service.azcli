# Make 100 requestes to the App Service with different Query Strings
for i in $(seq 1 100); do curl -s -w 'Request in progress '$i'\n'  -o /dev/null https://<AppService URL>?counter=$i ; sleep 0.1; done